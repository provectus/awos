# Tasks — Health Check Endpoint

- [x] **Slice 1: Health-check function returns `'ok'`**
  - [x] Sub-task: Create `src/health.py` containing a function `health_check()` that takes no arguments and returns the string `'ok'`. **[Agent: python-expert]**
  - [x] Sub-task: Add `tests/test_health.py` with `assert health_check() == 'ok'` and run `pytest tests/test_health.py` to confirm the function behaves as specified. **[Agent: python-expert]**
